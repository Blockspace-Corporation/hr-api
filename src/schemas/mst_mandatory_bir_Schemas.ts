export interface mst_mandatory_birRequestBody{
    amount_start: number;
    amount_end: number;
    employee_tax_percentage:number;
    employee_additional_amount: number;
}

export interface mst_mandatory_birID {
    id: number;
}